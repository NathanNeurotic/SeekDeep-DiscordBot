SeekDeep v3 context + web routing update

Adds short-term per-channel memory and better web search query selection.

Fixes:
- Follow-ups such as "their shape" can refer to recent messages.
- "Use the internet to infer the correct answer" searches the prior topic instead of the word "infer".
- /refine preserves concrete details and context better.
- Mention replies, /ask, /refine, /vision, and /image are stored in short-term memory.
